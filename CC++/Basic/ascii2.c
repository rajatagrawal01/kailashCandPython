#include<stdio.h>

int main(){
    char letter= 'B';
    if (letter=='A' || letter=='E' || letter=='I' || letter=='O' || letter=='U'){
        printf("It is a vowel");
    }
    else{
        printf("Consonent");
    }
    return 0;
}